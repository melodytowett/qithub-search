export class User {
 constructor(public login:string,public name:string, public avatar_url:string,
     public html_url:string, public followers:string, public following:string, public public_repos:string ){

 }
 
}
